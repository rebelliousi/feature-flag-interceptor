import { FeatureFlagInterceptor } from './feature-flag.interceptor';

describe('FeatureFlagInterceptor', () => {
  it('should be defined', () => {
    expect(new FeatureFlagInterceptor()).toBeDefined();
  });
});
